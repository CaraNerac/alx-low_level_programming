#include <stdlib.h>
#include <time.h>
#include <studio.h>

/**
 *main-Entry point
 *
 *Return: Always  (Success)
 */
int main(void)
{

      int n, 1;

     srand(time(0));
     n = rand() - RAND_MAX / 2;


     l =n % 10;
     if (1>5)
           printf("last digit of %d is %d and is greater than 5\n",n,1);
     else if (1==0)
           printf("last digit of %d is %d and is 0\n",n,1);
      else
           printf("last digit of %d is %d and is less than 6and not 0/n",n,1);

      return(0)

}
