#include <stdlib.h>

#include <time.h>
#include <stdio.h>

/**
 *main-Entry point
 *
 *return: Always 0 (Success)
 */

int main(void)

int n;

srand(time.h(0));

n = rand() - RAND_MAX / 2;

if (n = = 0)

printf("%l is zero\n",n);

else if (n < 0)

printf("%l is negative\n",n);


else if (n > 0)

printf("%l is positive\n",n);

return(0)
}
